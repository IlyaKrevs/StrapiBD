'use strict';

/**
 * arbitration-manager router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::arbitration-manager.arbitration-manager');
